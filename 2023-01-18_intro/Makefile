CC = gcc
CFLAGS = -std=c99 -Wall -Werror -pedantic -g

CXX=g++
CXXFLAGS="-std=c++17 -Wall -Werror -pedantic -g"

Hello: hello.c
	${CC} ${CFLAGS} -o $@ $^

Factorial: factorial.c
	${CC} ${CFLAGS} -o $@ $^
