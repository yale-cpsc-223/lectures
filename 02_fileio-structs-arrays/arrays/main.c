#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    int n = atoi(argv[1]);
    int nums[n];
    for (size_t i = 0; i < n; i++) 
    {
        nums[i] = i;
    }

    int factorials[n];
    for (int i = 0; i < n; i++) 
    {

    }
    return 0;
}
